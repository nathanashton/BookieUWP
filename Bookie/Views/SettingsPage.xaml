<Page
    x:Class="Bookie.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bookie.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <ListBox ItemsSource="{Binding Sources}" Width="300" SelectedItem="{Binding SelectedSource, Mode=TwoWay}" HorizontalAlignment="Center" Height="300" VerticalAlignment="Center" Background="Gray">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Path}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <TextBlock  VerticalAlignment="Bottom" Margin="0,0,0,70" Foreground="White" Text="{Binding SelectedSource.Path}" />
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
            <CommandBar.PrimaryCommands>
                <AppBarButton Foreground="White" Icon="Refresh" Label="Update" Command="{Binding UpdateCommand}" />

                <AppBarButton Icon="Add" Label="Import" Command="{Binding AddCommand}" />
                <AppBarButton Label="Remove" Icon="Remove" Command="{Binding RemoveCommand}" />
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>